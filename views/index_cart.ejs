<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>SnackZilla App</title>
</head>
<body>
    <%- include ('partials/header.ejs') %>
   
    <div class="container">
      <br>
      <div class="text-center"><h2>Register</h2></div>
      <br>

        <table class="table table-hover small text-center">
            <thead>
                <tr>
                    <th width="200">Order Item ID</th>
                    <th width="170">Order ID</th>
                    <th width="200">Product ID</th>
                    <th width="200">Quantity</th>
                    <th width="200">Edit</th>
                    <th width="200">Delete</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i =0; i < products.length; i++) { %>
                    <tr>
                        <td><%= products[i].Order_ItemID %></td>
                        <td><a href="/product/<%= products[i].OrderID %>"><%= products[i].name %></a></td>

                        <!-- if products not defined change to product-->

                        <td><%= products[i].productID %></td>
                        <td><%= products[i].Stock %></td>
                        <td><%= products[i].Price %></td>
                        <td><img src="<%= product[i].Image %>" alt="product image" width="150px" %></td>
                        <!-- Edit Link -->
                        <td><a href="/editProduct/<%= products[i].productId %>">Edit</a></td>
                        <!-- Delete Link -->
                        <td><a href="/deleteProduct/<%= products[i].productId %>" onclick="return confirm('Are you sure you want to delete this product?')">Delete</a></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
     </div>
 
</body>
</html>
    