<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>SnackZilla App</title>
</head>
<body>
    <%- include ('partials/header.ejs') %>
   
    <div class="container">
      <br>
      <div class="text-center"><h2>Our Catalogue</h2></div>
      <br>

        <table class="table table-hover small text-center">
            <thead>
                <tr>
                    <th width="200">Product ID</th>
                    <th width="170">Product Name</th>
                    <th width="200">Product Description</th>
                    <th width="200">Product Category</th>
                    <th width="200">Price</th>
                    <th width="200">Stock</th>
                    <th width="200">Image</th>
                    <th width="200">Edit</th>
                    <th width="200">Delete</th>
                </tr>
            </thead>
            <tbody>
                <% for (let i =0; i < products.length; i++) { %>
                    <tr>
                        <td><%= products[i].productID %></td>
                        <td><a href="/product/<%= products[i].productID %>"><%= products[i].name %></a></td>
                        <td><%= products[i].Description %></td>
                        <td><%= products[i].Category %></td>
                        <td><%= products[i].Price %></td>
                        <td><%= products[i].Stock %></td>
                        <td><img src="images/<%= products[i].images %>" alt="product image" width="150px" %></td>
                        <!-- Edit Link -->
                        <td><a href="/editProduct/<%= products[i].productID %>">Edit</a></td>
                        <!-- Delete Link -->
                        <td><a href="/deleteProduct/<%= products[i].productID %>" onclick="return confirm('Are you sure you want to delete this product?')">Delete</a></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
     </div>
</body>
</html>
    